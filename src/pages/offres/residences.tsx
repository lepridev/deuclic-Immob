import Layout from "@/ui/components/layout/Layout";
import React from "react";

const residences = () => {
  return (
    <Layout>
      <div>Residences meublées</div>
    </Layout>
  );
};

export default residences;
